import { FC, memo } from 'react'

interface FooterProps {

}

const Footer: FC<FooterProps> = memo(({ }) => {
    return (
        <footer className="w-full flex p-3 justify-center rounded-t-xl bg-black bg-opacity-15 relative bottom-0">
            sdsd
        </footer>
    )
})

export default Footer