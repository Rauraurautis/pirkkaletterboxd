import { expect, test } from 'vitest'

test('adds 1 + 2 to equal 3', () => {
    expect(4 + 5).toBe(9)
})


test('adds 5 + 22 to equal 27 lolollol', () => {
    expect(5 + 21).toBe(26)
})

test('adds 7 + 2 to equal 9', () => {
    expect(7 + 2).toBe(9)
})

test('adds 7 + 2 to equal 9', () => {
    expect(5).toBe(5)
})

test('adds 1 + 0 = 1', () => {
    expect(1).toBe(1)
})